#include<iostream>
using namespace std;
int main(){

    //print the numbers from 1 to 100
    // for(int count= 1; count<=100; count= count+1)
    // cout<<count;

    //print the numbers from 100 to 1
    // for(int count = 100; count>0; count = count-1)
    // cout<<count;

    //print the table of any numbers
     
    // int n;

    // cout << "Enter a positive integer: ";
    // cin >> n;

    // // run a loop from 1 to 10
    // // print the multiplication table
    // for (int i = 1; i <= 10; ++i) {
    //     cout << n << " * " << i << " = " << n * i << endl;
    // }

    //print the alphabets from A to Z in caps

    for(char ch = 'A' ; ch<='Z'; ch=ch+1 )
    cout<<ch<<endl;

    //same thing goes for small letters alphabets just put small a and small z.
    
    return 0;
}
    

   